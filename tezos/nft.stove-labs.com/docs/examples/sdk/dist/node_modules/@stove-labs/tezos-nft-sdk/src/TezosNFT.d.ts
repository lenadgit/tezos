export interface Config {
    serverAddress: string;
    contractAddress: string;
}
export interface Token {
    owner: string;
}
export declare class TezosNFT {
    config: Config;
    constructor(config: Config);
    getStorage(): Promise<any>;
    getContractOwner(): Promise<string>;
    getAllTokens(): Promise<Token[]>;
    getTokensCount(): Promise<number>;
    private prepareForOperationForging;
    private forgeOperation;
    forgeTransferTokenOperation(publicKeyHash: string, id: string, destination: string): Promise<string>;
}
export default TezosNFT;
